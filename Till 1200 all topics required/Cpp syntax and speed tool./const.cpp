#include <iostream>
using namespace std;

int main(){
           
ios::sync_with_stdio(false);
cin.tie(NULL);

const int x = 10;
//makes a variable read only after initiziation;
//works with; varaibels refreences pointers

//use const & for large parameters to avoid copies

//when 
//constexpre
//as const  is not strict enough. 
//if we want to make sure hte value is know befor the progremmee run 
// we use constexpr.

constexpr int x = 4;
// constexpr int y = rand() this will give erorr





  return 0;
}

